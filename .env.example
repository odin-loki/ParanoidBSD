# Copy to .env (gitignored) or use secrets/api-keys (also gitignored).
# Stub with the same fields: secrets/api-keys.example
# Never commit real keys.
DEEPSEEK_API_KEY=
MOONSHOT_API_KEY=
# Optional overrides
# DEEPSEEK_BASE_URL=https://api.deepseek.com
# MOONSHOT_BASE_URL=https://api.moonshot.ai/v1
# KIMI_REASONING_EFFORT=low
