/*
 * This file was generated by the mknodes program.
 */

      if (n == NULL)
	    return NULL;
      new = state->block;
      state->block = (char *)state->block + nodesize[n->type];
      switch (n->type) {
      case N539:
	    break;
      };
      new->type = n->type;
