hbsd/src/lib/msun/ld128/s_expl.c	expl()/expm1l() depend on k_expl.h internals not present in this batch (the 128-entry `tbl[]`, `__k_expl()`, INV_L/L1/L2, A2..A10, INTERVALS/LOG2_INTERVALS) and on the binary128 `union IEEEl2bits`/SET_LDBL_EXPSIGN layout; this host's long double is x87 80-bit (LDBL_MANT_DIG=64), so no faithful port or oracle is possible without fabricating the missing table and kernel.
hbsd/src/lib/msun/ld128/e_lgammal_r.c	sin_pil()/lgammal_r() require the ld128 kernels __kernel_sinl()/__kernel_cosl() (separate translation units, not in this batch) and EXTRACT_LDBL128_WORDS on a 113-bit-mantissa binary128 representation; this host's long double is x87 80-bit, so the word extraction would read indeterminate padding and the kernels would have to be invented.
