export module pbsd.lib.libc.db.test.hash.tests.b0185;

/*
 * Batch b0185 sources (tseq.c, tcreat3.c, tread2.c, tverify.c) are Berkeley
 * DB hash-table integration-test drivers.  Each file defines only main() and
 * depends on <db.h>, dbopen(), and either stdin or an on-disk "hashtest"
 * database.  See skipped.txt.  Nothing was faithfully portable, so this
 * namespace exports nothing.
 */
export namespace pbsd::lib_libc_db_test_hash_tests::b0185 {
}
