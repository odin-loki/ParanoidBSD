// PBSD -- mechanical C++23 port of timespec_getres.c, proven equivalent to
// the original C by LLVM IR comparison. Generated by pbsd.py.
module;
#include <time.h>

export module pbsd.lib.libc.gen.timespec_getres;

export namespace pbsd::lib::libc::gen::timespec_getres {

/*-
 * Copyright (c) 2023 Dag-Erling Smørgrav
 *
 * SPDX-License-Identifier: BSD-2-Clause
 */



int
timespec_getres(struct timespec *ts, int base)
{

	switch (base) {
	case TIME_UTC:
		if (clock_getres(CLOCK_REALTIME, ts) == 0)
			return (base);
		break;
	case TIME_MONOTONIC:
		if (clock_getres(CLOCK_MONOTONIC, ts) == 0)
			return (base);
		break;
	}
	return (0);
}

}  // namespace pbsd::lib::libc::gen::timespec_getres
