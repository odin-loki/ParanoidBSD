#
# HARDENEDBSD -- Main HardenedBSD kernel configuration for i386.
#
# The hardening policy is sys/conf/std.hardenedbsd and is the same on every
# architecture. Anything below the include is an overload for i386
# specifically, and has to justify itself as such.

include GENERIC
include "std.hardenedbsd"

ident		HARDENEDBSD
