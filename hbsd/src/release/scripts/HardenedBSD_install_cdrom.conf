#
#
# The pkg(8) repository configuration file for the installation DVD.
#

HardenedBSD_install_cdrom: {
  url: "file:///dist/packages/${ABI}",
  mirror_type: "none",
  enabled: yes
}

HardenedBSD: {
  enabled: no
}

