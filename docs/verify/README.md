# Verify docs

The method lives in [`tools/verify/README.md`](../../tools/verify/README.md).
Confirmed defects live in [`docs/security/UB_FINDINGS.md`](../security/UB_FINDINGS.md).
This directory is what a *run* left behind: numbers, packs, and the queue a person still has to read.

| Run | What it is |
|---|---|
| [sweep-2026-09-18](sweep-2026-09-18/README.md) | Whole-tree C sweep on WSL (`sys` … `libexec`), then KDE C++. Latest. |

A digest is a summary. A pack is evidence. Neither is a list of bugs.
